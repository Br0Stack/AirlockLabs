# Threadline API

Run:

```bash
pip install -r apps/api/requirements.txt
uvicorn threadline_api.main:app --reload --app-dir apps/api
```

Implements MVP routes for workspaces, document upload, event extraction, timeline editing, and workspace chat.
